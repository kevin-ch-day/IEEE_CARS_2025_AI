\section{Methodology}

\subsection{Study Design and Evidence Window}

This study performs a descriptive, build-aligned analysis of 15 Android consumer applications. It examines three questions: the static exposure present in selected app builds, the runtime behavior observed for those builds, and the relationships or divergences between the two evidence layers. The unit of inference is the selected app-build group, defined by package name, version code and name, base APK hash, split-APK identity, contributing static run, and selected dynamic runs.

Collection was conducted on a non-root Motorola moto g 5G 2024 running Android 15. Evidence was selected from a fixed window spanning June 26 through July 9, 2026 UTC. Static results and dynamic runs were eligible only when they could be linked to the selected package and build group and passed identity, validity, signal-sufficiency, and quality-assurance checks. Applications were grouped by primary user-facing function into News, Social Media, Messaging, and Professional Networking categories. The categories are used for descriptive visualization only and are not treated as statistical strata. The non-root setting avoided application modification, privileged instrumentation, TLS interception, and payload inspection.

\subsection{Static and Runtime Evidence}

Static analysis used the harvested base and split APK artifacts associated with each selected build. The reported variables include dangerous permissions, high- and medium-severity findings, unguarded exported components, network- and storage-related findings, privacy and platform findings, MASVS-aligned categories, and supported SDK or service indicators. These measurements represent descriptive exposure indicators rather than calibrated exploitability, compliance, or risk scores.

Runtime analysis used package-associated evidence packs and PCAP-derived metadata. Captures were classified as strict-idle, quiescent foreground (QFG), or interactive. Strict-idle captures represent quiet foreground baselines; QFG captures represent no-touch foreground periods in which app-driven activity continued; and interactive captures exercise user-facing behavior. Runtime variables include capture count and duration, PCAP volume, packet rate, observed domains, service families, protocol prevalence, and baseline-relative changes where comparable baseline and interactive evidence were available.

\subsection{Build Selection and Evidence Alignment}

For each application, ScytaleDroid selected one eligible build group using package and version identity, base APK hash, static- and dynamic-run availability, PCAP state, recency, signal sufficiency, and quality-assurance status. Static and runtime observations were integrated only when they referred to the same selected build group. Evidence with mismatched build identity, missing static or dynamic support, invalid QA state, or insufficient signal was excluded or deferred. Algorithm~\ref{alg:build-aligned-selection} summarizes the deterministic selection and reporting procedure.

The 14-day window serves as a provenance control rather than a claim that every application remained on the same installed version throughout the study. A later app-store update did not invalidate evidence collected from a recent, fully identified build; it instead created a candidate for future longitudinal comparison.

\begin{algorithm}[!t]
\caption{Build-Aligned Static-Runtime Evidence Selection}
\label{alg:build-aligned-selection}
\begin{algorithmic}[1]
\REQUIRE cohort $A$, 14-day window $W$, static set $S$, dynamic set $D$, qualification policy $P$
\ENSURE aligned app-build rows, excluded/deferred rows, source data, checksums
\FOR{each application $a \in A$}
  \STATE retrieve candidate dynamic runs for $a$ within $W$
  \STATE apply identity, evidence-validity, signal, PCAP, and QA requirements from $P$
  \STATE group qualifying runs by normalized package, version code, base APK SHA-256, and artifact/build-set identity
  \STATE identify build groups satisfying runtime evidence requirements
  \STATE select one group using the canonical deterministic build-ranking policy
  \STATE resolve static evidence in $S$ for the same selected build
  \IF{static and dynamic identities do not align}
    \STATE exclude or defer $a$; persist the reason; continue
  \ENDIF
  \STATE preserve strict-idle, QFG, and interactive classes separately
  \STATE compute static exposure and runtime behavior summaries
  \STATE add the aligned app-build row and provenance
\ENDFOR
\STATE compute descriptive cross-layer profiles and app-level associations
\STATE export aligned rows, excluded/deferred rows, source data, and checksums
\end{algorithmic}
\end{algorithm}

\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{figures/fig1_study_pipeline.pdf}
\caption{Study pipeline for build-aligned static and runtime evidence. The qualification gate excludes mismatched builds, missing evidence, and invalid or insufficient runs before integrated analysis.}
\label{fig:study-pipeline}
\end{figure}

\subsection{Integrated Analysis and Reproducibility}

Integrated analysis compares app-level static exposure indicators with app-level runtime observations. The study reports descriptive counts, evidence-class medians, baseline-relative packet-rate changes, and exploratory Spearman associations. Cross-layer profiles are constructed using a transparent median split: higher static exposure is determined from high- and medium-severity finding volume greater than or equal to the cohort median, while higher runtime activity is determined from interactive domain count greater than or equal to the cohort median. Equality is assigned to the higher group. These profiles support comparative interpretation but do not constitute a composite risk score or predictive model.

PCAP processing is limited to metadata and derived transport indicators. Message bodies, cookies, authorization headers, private account content, and decrypted TLS payloads are not inspected. Reproducibility artifacts include the selected-build manifest, source CSV files, generated \LaTeX{} tables, figure source data, inclusion and exclusion records, and SHA-256 checksums for generated assets.

\subsection{Limitations}

The study is limited to 15 applications, one Android 15 physical device, one collection environment, selected interaction scenarios, and the available static detectors. Runtime observations are scenario-bounded and do not establish exhaustive application behavior, while unequal capture depth may affect the precision of app-level comparisons. The non-root, no-MITM design preserves a clear privacy boundary but prevents inspection of encrypted payload semantics.

Static finding counts are tool-dependent exposure indicators rather than formal measures of exploitability, MASVS compliance, or privacy harm. Likewise, observed runtime activity does not demonstrate malicious behavior, and limited activity does not establish low risk. The integrated analysis therefore characterizes the static and runtime posture of the selected builds without inferring causation, predicting future behavior, or generalizing the results to all Android devices and app versions.
