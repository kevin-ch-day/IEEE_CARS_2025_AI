# Static measurement audit

Cohort-wide alias title dropped: `Exported activity alias without permission`.
955 unguarded-export findings = High/Medium rows whose title contains 'without permission' or 'weak guard'. These are finding rows, not unique components.
